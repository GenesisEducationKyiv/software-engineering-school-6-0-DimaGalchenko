#!/bin/sh

echo "Running lint on staged files..."

STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM -- '*.js')

if [ -z "$STAGED_FILES" ]; then
  exit 0
fi

npx eslint $STAGED_FILES

if [ $? -ne 0 ]; then
  echo "Lint errors found. Please fix them before committing."
  exit 1
fi
